import { makeStyles, createStyles } from '@mui/styles';



const useStyles = makeStyles(() => {
    return createStyles({
      logout : {
        color: 'white'
      },
      wrapper : {
        color: 'white',
        textAlign: 'end',
        paddingRight: '1rem'
      }
    })
  })

  export default useStyles;

  

